[pytest]
markers =
    integration: end-to-end tests that ingest real PDFs (slow; opt in with -m integration)
# Unit tests are the default. Run the required-cases suite with:
#   pytest -m integration
# or every test with:
#   pytest -m ""
addopts = -q -m "not integration"
